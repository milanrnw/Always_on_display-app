<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- NEW: Permission to draw over the lock screen -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW"/>
    <!-- NEW: Permission to run a service in the foreground (required for long-running tasks) -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    <!-- NEW: Permission to start the service when the phone boots up (optional but good) -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>

    <application
        android:label="artful_aod"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
                <!-- REPLACE THE ENTIRE ACTIVITY BLOCK WITH THIS ONE -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        
        <!-- NEW: Declare our AOD background service -->
        <service
            android:name=".AodService"
            android:enabled="true"
            android:exported="false" />
            
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
</manifest>